class SelectedText {
    initalRange: Range
    releasedRange: Range
    constructor(initalRange: Range, releasedRange: Range) {
        this.initalRange = initalRange
        this.releasedRange = releasedRange
    }
}