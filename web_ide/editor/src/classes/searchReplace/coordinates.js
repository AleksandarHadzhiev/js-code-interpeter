export default class Coordinates {
    /**
     * 
     * @param {Number} width 
     * @param {Number} left 
     * @param {Number} top 
     */
    constructor(width, left, top) {
        this.width = width
        this.left = left
        this.top = top
    }
}